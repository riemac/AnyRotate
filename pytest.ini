[pytest]
# Restrict discovery to the tests folder
testpaths = tests

# Be explicit to avoid accidental plugin loading from environment (double safety)
# If some plugin name appears, you can disable it here, e.g.,
# -p no:launch_testing
addopts = -q
